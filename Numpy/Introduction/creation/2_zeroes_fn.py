# with default values
# np.zeros(shape, dtype=float, order='C')
# np.ones(shape, dtype=float, order='C')
# np.zeros(shape) (3) fir 1d, (3,3) for 2d, (3,3,3) for 3d

import numpy as np

zeroes_array = np.zeros(3)
print(zeroes_array)