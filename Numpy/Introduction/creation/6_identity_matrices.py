# creating identity matrices in numpy
# identity matrix -> square matrix with 1s on the diagonal and 0s elsewhere, mtlb square matrix bnega.
# np.identity(n)

import numpy as np

identity_matrix = np.eye(3)
print(identity_matrix)